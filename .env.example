# Orchestrator configuration
# Mode options: manual | delimited | api
LLM_MODE=manual

# Docker container name for the agent (from docker-compose.yml)
AGENT_CONTAINER=breakout_agent

# Optional: where to store run logs (meta.json + events.jsonl)
# RUNS_DIR=/absolute/path/to/runs

# Delimited mode block separator (used when LLM_MODE=delimited)
BLOCK_DELIM=---

# LLM API settings (used when LLM_MODE=api)
LLM_PROVIDER=openai
# Preferred model; falls back to OPENAI_MODEL or default
LLM_MODEL=gpt-4o-mini
# Alternative variable some setups use
# OPENAI_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0
LLM_MAX_TOKENS=512

# Required for api mode
# OPENAI_API_KEY=sk-...
